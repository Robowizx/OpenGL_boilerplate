{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "opengl build",
            "type": "shell",
            "command": "g++ -I ./includes -L ./libs -framework OpenGL -lglfw.3.3 -lGLEW.2.1.0 -o app app.cpp",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
